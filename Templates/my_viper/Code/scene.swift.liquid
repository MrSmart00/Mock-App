{% include 'header' %}

import Foundation

enum {{ module_info.name }} {
    public struct Context {
        public init() {}
    }

    public enum Wireframe {

    }
}

protocol {{ module_info.name }}InteractorProtocol {

}

public enum {{ module_info.name }}Message {

}

protocol {{ module_info.name }}PresenterProtocol {
    func dispatch(_ message: {{ module_info.name }}Message)
}
