{% include 'header' %}

import Foundation
import UIKit
import Combine
import Common

final class {{ module_info.name }}ViewController: UIViewController, Injectable {
    struct Dependency {
        let presenter: {{ module_info.name }}PresenterProtocol
    }

    private var dependency: Dependency!

    private var cancellables = Set<AnyCancellable>()

    override func viewDidLoad() {
        super.viewDidLoad()
    }

    func inject(dependency: {{ module_info.name }}ViewController.Dependency) {
        self.dependency = dependency
    }
}
