import Foundation

{% for key, value in argument %}
enum {{ value }} {
    public struct Context {
        public init() {}
    }

    public enum Wireframe {

    }
}

protocol {{ value }}InteractorProtocol {

}

public enum {{ value }}Message {

}

protocol {{ value }}PresenterProtocol {
    func dispatch(_ message: {{ value }}Message)
}
{% endfor %}
